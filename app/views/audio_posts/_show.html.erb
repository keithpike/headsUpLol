
  <div class='postHeader'>
    <div class='postHeaderLeft'>
      <a href='#'>
        <span class='postTitle'>Audio</span>
      </a>
    </div>

    <div class='postHeaderRight'>

    </div>

    <div class='postInfo'>
      <p class='postSubheader'><%= current_user.username %></p>
      <p class='postSubheader'><%= post.created_at.strftime("%B %d, %Y") %></p>
    </div>
  </div>

  <div class='postContent'>

    <div class='mediaWrap'>
      <% embeddable = get_embed_link(post.media_url).first.html %>
      <% if embeddable %>
       <%= embeddable.html_safe %>
      <% else %>
        <img src='https://s3-us-west-1.amazonaws.com/headsuplolimages/404preview.png'>404 error Suppose you input the wrong url
        <img>
      <% end %>
    </div>

    <div class='contentWrap'>
      <p class='postDescription'><%= post.description %></p>
    </div>

  </div>

  <div class='postFooter'>
  </div>

