# Pusher.oembed_key = ENV["oembed_key"]
# Pusher.oembed_secret = ENV["oembed_secret"]
# Pusher.filepicker_key = ENV['filepicker_key']
# Pusher.filepicker_secret = ENV["filepicker_secret"]

# embedly_api = Embedly::API.new({:key => ENV["oembed_key"]})